spring.jpa.hibernate.ddl-auto=update
    none → Hibernate tablo oluşturmaz
    validate → Var olan tabloyu kontrol eder
    update → zabloyu günceller, yeni alan ekler
    create → Her çalışmada tabloları silip yeniden oluşturur
    create-drop → Uygulama kapanınca tabloları siler